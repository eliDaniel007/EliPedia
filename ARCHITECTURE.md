# 🏗️ Architecture du Projet ELIPEDIA

Ce document explique l'architecture technique du projet.

## 🎯 Vue d'Ensemble

```
┌─────────────────────────────────────────────────────────────┐
│                     ELIPEDIA - List Master                   │
│              Application Web de Jeux de Listes               │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Architecture en 3 Couches

```
┌──────────────────────────────────────────────────────────────┐
│                      COUCHE PRÉSENTATION                      │
│                     (Frontend - À venir)                      │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   Mode      │  │    Mode     │  │    Mode     │         │
│  │  Encyclopédie│  │  Jeu Solo  │  │ Multijoueur │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                               │
│         React + Next.js / Vue + Nuxt.js                      │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ HTTP GET/POST
                         │ WebSocket (pour le multi)
                         ↓
┌──────────────────────────────────────────────────────────────┐
│                      COUCHE MÉTIER (API)                      │
│                    Backend Node.js + Express                  │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │  /api/       │  │  /api/lists  │  │  /api/search │       │
│  │  categories  │  │              │  │              │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                               │
│  ┌──────────────────────────────────────────────────┐        │
│  │          Logique de Jeu (à venir)                │        │
│  │  - Validation des réponses                       │        │
│  │  - Gestion des parties multijoueur               │        │
│  │  - IA pour le mode solo                          │        │
│  └──────────────────────────────────────────────────┘        │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ SQL Queries (pg module)
                         ↓
┌──────────────────────────────────────────────────────────────┐
│                    COUCHE DONNÉES                             │
│                  PostgreSQL Database                          │
│                                                               │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐           │
│  │ category │──┐   │   list   │──┐   │   item   │           │
│  ├──────────┤  │   ├──────────┤  │   ├──────────┤           │
│  │ id       │  │   │ id       │  │   │ id       │           │
│  │ name     │  │   │ name     │  │   │ name     │           │
│  └──────────┘  │   │ desc...  │  │   │ list_id  │           │
│                └──→│ cat_id   │  │   │ metadata │           │
│                    └──────────┘  └──→└──────────┘           │
└──────────────────────────────────────────────────────────────┘
                         ↑
                         │
                         │ npm run seed
                         │
┌──────────────────────────────────────────────────────────────┐
│                   GESTION DES DONNÉES                         │
│              Fichiers JSON Locaux (/data)                     │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │geographie   │  │  cinema.json│  │ science.json│          │
│  │   .json     │  │             │  │             │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│                                                               │
│          📝 Vous éditez ces fichiers en local                │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 Flux de Données Détaillé

### 1️⃣ Workflow "Data as Code"

```
┌─────────────┐
│  Developer  │  👨‍💻 Vous
└──────┬──────┘
       │
       │ 1. Éditer les fichiers JSON
       ↓
┌─────────────┐
│ /data/*.json│
└──────┬──────┘
       │
       │ 2. npm run seed
       ↓
┌─────────────┐
│   seed.js   │  Script de synchronisation
└──────┬──────┘
       │
       │ 3. INSERT/UPDATE dans PostgreSQL
       ↓
┌─────────────┐
│ PostgreSQL  │  Base de données
└─────────────┘
```

### 2️⃣ Flux de Requête Utilisateur (Mode Encyclopédie)

```
┌─────────────┐
│   User      │  👤 Utilisateur
└──────┬──────┘
       │
       │ 1. Ouvre le site
       ↓
┌─────────────┐
│  Frontend   │  React/Vue
└──────┬──────┘
       │
       │ 2. GET /api/categories
       ↓
┌─────────────┐
│   Express   │  Backend
│   Routes    │
└──────┬──────┘
       │
       │ 3. SELECT * FROM category
       ↓
┌─────────────┐
│ PostgreSQL  │
└──────┬──────┘
       │
       │ 4. JSON Response
       ↓
┌─────────────┐
│  Frontend   │  Affiche les catégories
└──────┬──────┘
       │
       │ 5. Rendu HTML
       ↓
┌─────────────┐
│    User     │  Voit les catégories
└─────────────┘
```

### 3️⃣ Flux de Jeu (Mode Solo - À venir)

```
┌─────────────┐
│   Player    │
└──────┬──────┘
       │
       │ 1. Choisit une liste
       ↓
┌─────────────┐
│  Frontend   │
│ (Game UI)   │
└──────┬──────┘
       │
       │ 2. GET /api/lists/:id (récupérer la liste)
       ↓
┌─────────────┐
│   Express   │
└──────┬──────┘
       │
       │ 3. SELECT * FROM item WHERE list_id = :id
       ↓
┌─────────────┐
│ PostgreSQL  │
└──────┬──────┘
       │
       │ 4. Retourne tous les items
       ↓
┌─────────────┐
│  Frontend   │  Stocke les bonnes réponses
│ (Game Logic)│
└──────┬──────┘
       │
       │ 5. Player tape une réponse
       ↓
┌─────────────┐
│  Frontend   │  Validation côté client
│ (Validation)│  (pour le mode solo)
└──────┬──────┘
       │
       │ 6. Résultat (correct / incorrect)
       ↓
┌─────────────┐
│   Player    │  Feedback visuel
└─────────────┘
```

### 4️⃣ Flux Multijoueur (À venir - WebSockets)

```
┌──────────┐           ┌──────────┐
│ Player 1 │           │ Player 2 │
└────┬─────┘           └────┬─────┘
     │                      │
     │ WebSocket Connect    │ WebSocket Connect
     ↓                      ↓
┌────────────────────────────────┐
│     WebSocket Server           │
│    (Socket.io / WS native)     │
└────────────┬───────────────────┘
             │
             │ Broadcast events
             ↓
     ┌───────────────┐
     │  Game Room    │  État de la partie
     │  (in memory)  │
     └───────────────┘
```

## 🗂️ Modèle de Données

### Relations entre tables

```
category (1) ──────< (N) list (1) ──────< (N) item
   ↓                      ↓                    ↓
  id                     id                   id
 name               category_id            list_id
                         name                name
                      description          metadata
```

### Exemple de données liées

```
Category: "Géographie" (id: 1)
    │
    ├── List: "Pays d'Afrique" (id: 101, category_id: 1)
    │       │
    │       ├── Item: "Nigeria" (id: 5001, list_id: 101)
    │       ├── Item: "Égypte" (id: 5002, list_id: 101)
    │       └── Item: "Kenya" (id: 5003, list_id: 101)
    │
    └── List: "Capitales d'Europe" (id: 102, category_id: 1)
            │
            ├── Item: "Paris" (id: 6001, list_id: 102)
            └── Item: "Madrid" (id: 6002, list_id: 102)
```

## 🔧 Stack Technique

### Backend (Actuel)
```
┌─────────────────────────────────────┐
│         Node.js v18+                │
│                                     │
│  ┌───────────────────────────────┐ │
│  │      Express 4.18             │ │
│  │  (Framework HTTP)             │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │      pg (node-postgres)       │ │
│  │  (Client PostgreSQL)          │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │      dotenv                   │ │
│  │  (Variables d'env)            │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │      cors                     │ │
│  │  (Cross-Origin)               │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

### Base de Données
```
┌─────────────────────────────────────┐
│     PostgreSQL 14+                  │
│                                     │
│  - Requêtes SQL optimisées          │
│  - Index sur les clés étrangères    │
│  - JSONB pour métadonnées flexibles │
│  - Contraintes d'intégrité          │
└─────────────────────────────────────┘
```

### Frontend (À venir)
```
Option 1:                 Option 2:
┌──────────────┐         ┌──────────────┐
│ React 18+    │         │ Vue 3+       │
│ Next.js 14+  │         │ Nuxt.js 3+   │
└──────────────┘         └──────────────┘
```

## 🚀 Déploiement (Production)

### Architecture de Déploiement

```
┌───────────────────────────────────────────────────────────┐
│                        Internet                            │
└────────────────────┬──────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ↓                         ↓
┌───────────────┐         ┌───────────────┐
│   Frontend    │         │   Backend     │
│   (Vercel)    │────────→│   (Render)    │
│               │  API    │               │
└───────────────┘         └───────┬───────┘
                                  │
                                  │ SQL
                                  ↓
                          ┌───────────────┐
                          │  PostgreSQL   │
                          │  (Render DB)  │
                          └───────────────┘
```

## 📊 Performances

### Optimisations Actuelles

1. **Index de base de données**
   - Index sur `category.name`
   - Index sur `list.category_id` et `list.name`
   - Index sur `item.list_id` et `item.name`
   - Index GIN sur `item.metadata` (pour recherches JSON)

2. **Connection Pooling**
   - Pool de 20 connexions PostgreSQL
   - Timeout : 2 secondes
   - Idle timeout : 30 secondes

3. **Cache (À implémenter)**
   - Redis pour cache des listes populaires
   - Cache HTTP côté client

## 🔐 Sécurité

### Mesures de Sécurité Actuelles

1. **Variables d'environnement**
   - Mots de passe dans `.env` (non versionné)
   
2. **Validation SQL**
   - Requêtes paramétrées (protection contre SQL injection)

3. **CORS**
   - Origines autorisées configurables

### À Implémenter

- [ ] Rate limiting
- [ ] Authentification JWT (pour le mode multi)
- [ ] Validation des inputs côté serveur
- [ ] HTTPS en production

## 🎯 Évolution Future

### Phase 2 : Frontend
- Interface "Encyclopédie"
- Interface "Jeu Solo"

### Phase 3 : Multijoueur
- WebSockets (Socket.io)
- Gestion des salons de jeu
- Lobbies

### Phase 4 : Fonctionnalités Avancées
- Statistiques des joueurs
- Classements (leaderboards)
- Système de points
- Achèvements (achievements)

---

**Architecture évolutive et scalable !** 🚀

